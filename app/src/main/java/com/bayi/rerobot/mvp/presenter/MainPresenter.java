package com.bayi.rerobot.mvp.presenter;

import com.bayi.rerobot.mvp.view.MainView;

public class MainPresenter extends BasePresenter<MainView> {
    public MainPresenter(MainView view) {
        mView = view;
    }
}
