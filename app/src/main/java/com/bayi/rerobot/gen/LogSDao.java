package com.bayi.rerobot.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.bayi.rerobot.greendao.LogS;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "LOG_S".
*/
public class LogSDao extends AbstractDao<LogS, Long> {

    public static final String TABLENAME = "LOG_S";

    /**
     * Properties of entity LogS.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Ymd = new Property(1, String.class, "ymd", false, "YMD");
        public final static Property Hm = new Property(2, String.class, "hm", false, "HM");
        public final static Property Msg = new Property(3, String.class, "msg", false, "MSG");
    }


    public LogSDao(DaoConfig config) {
        super(config);
    }
    
    public LogSDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"LOG_S\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"YMD\" TEXT," + // 1: ymd
                "\"HM\" TEXT," + // 2: hm
                "\"MSG\" TEXT);"); // 3: msg
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"LOG_S\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, LogS entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String ymd = entity.getYmd();
        if (ymd != null) {
            stmt.bindString(2, ymd);
        }
 
        String hm = entity.getHm();
        if (hm != null) {
            stmt.bindString(3, hm);
        }
 
        String msg = entity.getMsg();
        if (msg != null) {
            stmt.bindString(4, msg);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, LogS entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String ymd = entity.getYmd();
        if (ymd != null) {
            stmt.bindString(2, ymd);
        }
 
        String hm = entity.getHm();
        if (hm != null) {
            stmt.bindString(3, hm);
        }
 
        String msg = entity.getMsg();
        if (msg != null) {
            stmt.bindString(4, msg);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public LogS readEntity(Cursor cursor, int offset) {
        LogS entity = new LogS( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // ymd
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // hm
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3) // msg
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, LogS entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setYmd(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setHm(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setMsg(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(LogS entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(LogS entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(LogS entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
